{
  "name": "@lb-team/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "db:generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "db:migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "db:migrate:deploy": "prisma migrate deploy",
    "push": "prisma db push",
    "db:push": "prisma db push",
    "seed": "tsx prisma/seed.ts",
    "db:seed": "tsx prisma/seed.ts",
    "remove-demo-students": "tsx prisma/remove-demo-students.ts",
    "remove-all-students": "tsx prisma/remove-all-students.ts",
    "setup:local": "bash ../../scripts/setup-local.sh"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
